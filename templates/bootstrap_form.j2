{{ form.hidden_tag() }} {% for field in form if
field.widget.input_type != 'hidden' %}
<div class="mt-2">
  {{ field.label (class_='form-label') }} {{ field
  (class_='form-control')}} {% for error in field.errors %} {{ error }} {%
  endfor %}
</div>
{% endfor %}

